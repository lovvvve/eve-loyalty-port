## 语言

与用户的交互、工作记录以及新建或更新的文档均使用简体中文，除非用户明确要求其他语言。代码、命令、文件路径、API 名称及专有术语保留其原始形式。

## Agent skills

### Issue tracker

问题和规格以本地 Markdown 文件形式保存在 `.scratch/<feature-slug>/` 下。详见 `docs/agents/issue-tracker.md`。

### Triage labels

分诊采用五个默认规范标签字符串。详见 `docs/agents/triage-labels.md`。

### Domain docs

本仓库采用单上下文（single-context）领域文档布局。详见 `docs/agents/domain.md`。
